# Postgres(serberはdb)
POSTGRES_DB=gym_directory
POSTGRES_USER=appuser
POSTGRES_PASSWORD=apppass
POSTGRES_PORT=5432

# App
APP_PORT=8000
# カンマ区切りで CORS 許可するオリジンを指定（未指定の場合、開発環境では 3000 番を許可）
# ALLOW_ORIGINS=http://127.0.0.1:3000,http://localhost:3000

# DB URL for SQLAlchemy/Alembic
DATABASE_URL=postgresql+psycopg2://appuser:apppass@db:5432/gym_directory

# Scoring weights
SCORE_W_FRESH=0.6
SCORE_W_RICH=0.4
FRESHNESS_WINDOW_DAYS=365

# Frontend
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
NEXT_PUBLIC_AUTH_MODE=stub
# OAuth mode (example placeholders)
# NEXT_PUBLIC_OAUTH_PROVIDER=github
# NEXT_PUBLIC_OAUTH_CLIENT_ID=
# NEXT_PUBLIC_OAUTH_REDIRECT_URI=http://127.0.0.1:3000/auth/callback
